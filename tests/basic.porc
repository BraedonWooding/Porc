# Just a basic Porc test
# Bug x = ((n) => {2})
a, b := [1, 2]

main :: () => {
  x :: () => 2;

  keb = ((n) => 2);
  keb = (2,);

  zl :: () => {= 2;}

  9 + println(fib("hello", 2))(5) * 2 ** 2;
  x = 2;
  if (let x := 2) {
    = x;
  }
}

k :: () => {

}
